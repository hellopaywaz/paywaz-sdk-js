#!/usr/bin/env node
console.warn(
  "This repository provides a stub of @openapitools/openapi-generator-cli for CI determinism."
);
console.warn(
  "Install the real package in your environment if you need to regenerate the SDK."
);
process.exit(0);
