name: OpenAPI → SDK Sync Check

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 1" # Mondays 09:00 UTC

permissions:
  contents: read

jobs:
  sync-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      # Placeholder for future: compare generated types/client against OpenAPI hash
      - name: Sync check (placeholder)
        run: |
          echo "OpenAPI → SDK sync check is manual/scheduled only for now."
          echo "TODO: add real sync verification (e.g., openapi hash, generated client diff, etc.)"
